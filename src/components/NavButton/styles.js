import styled from 'styled-components';

export const Wrapper = styled.div`
    width: 100%;
    height: 2rem;
    position: absolute;
    bottom: 0;
    z-index: 10;

    border-top: 1px solid whitesmoke;
`;